{% extends 'base.html.twig' %}

{% block title %}Currencies{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            Offset
        </div>
        <div class="col-6">
            <table class="table table-dark">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Code</th>
                    <th scope="col">Rates</th>
                </tr>
                </thead>
                <tbody>
                {% for key,entity in entities %}
                    <tr>
                        <th scope="row">{{ 1 + key }}</th>
                        <td>{{ entity.code }}</td>
                        <td>({{ entity.name }})</td>
                        <td>
                            <a href="{{ path('rate_diagram', {'currencyId': entity.id}) }}" title="{{ entity.name }}" class="btn btn-primary">
                                View
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}